<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xmh.leave" >

    <uses-sdk android:minSdkVersion="8" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/h001"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ErrorActivity" >
        </activity>
        <activity android:name=".TeacherActivity" >
        </activity>
        <activity android:name=".AllListActivity" >
        </activity>
        <activity android:name=".UnListActivity" >
        </activity>
        <activity android:name=".DoListActivity" >
        </activity>
        <activity android:name=".AskInfoActivity" >
        </activity>
        <activity android:name=".AskActivity" >
        </activity>

    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>
